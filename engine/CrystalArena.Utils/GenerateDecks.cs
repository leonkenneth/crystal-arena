namespace CrystalArena.Utils
{
    using System;

    public class GenerateDecks : Task
    {
        public override bool Execute(Arguments arguments)
        {
            Console.WriteLine("GenerateDecks is no longer available (Media/ResourceFolder removed).");
            return false;
        }

        public override void Usage()
        {
            Console.WriteLine("GenerateDecks is no longer available (Media/ResourceFolder removed).");
        }
    }
}
