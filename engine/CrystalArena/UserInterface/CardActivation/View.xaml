<UserControl x:Class="CrystalArena.UserInterface.CardActivation.View"
             xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity" xmlns:cal="clr-namespace:Caliburn.Micro;assembly=Caliburn.Micro" xmlns:UserInterface="clr-namespace:CrystalArena.UserInterface"
             xmlns:CrystalArena="clr-namespace:CrystalArena" xmlns:Messages="clr-namespace:CrystalArena.Events">             
    <Border>
        <Border.Resources>
        <DataTemplate DataType="{x:Type CrystalArena:CastRule}">
                <ContentControl Content="{Binding SourceCard}"  />
        </DataTemplate>

        <DataTemplate DataType="{x:Type CrystalArena:Card}">
                <ContentControl Content="{Binding }"  />
        </DataTemplate>

        <DataTemplate DataType="{x:Type CrystalArena:ActivatedAbility}">
            <ContentControl Content="{Binding}" ContentTemplate="{StaticResource Ability}" />
        </DataTemplate>

        <DataTemplate DataType="{x:Type CrystalArena:TriggeredAbility}">
            <ContentControl Content="{Binding}" ContentTemplate="{StaticResource Ability}" />
        </DataTemplate>

        <DataTemplate DataType="{x:Type Messages:AbilityActivatedEvent}">
                <ContentControl Content="{Binding Ability}">
                    <i:Interaction.Triggers>                                   
                    </i:Interaction.Triggers>
                </ContentControl>                           
        </DataTemplate>
            <DataTemplate DataType="{x:Type Messages:SpellCastEvent}">
                <ContentControl Content="{Binding Card}">
                    <i:Interaction.Triggers>                  
                    </i:Interaction.Triggers>
                </ContentControl>
            </DataTemplate>
        </Border.Resources>

        <Grid Width="300">
            <Grid.RowDefinitions>
                <RowDefinition Height="36" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Border Grid.Row="0" Background="{StaticResource BarBackground}">
                <StackPanel Margin="10,0,10,0" Orientation="Horizontal">
                    <StackPanel.Resources>
                        <Style  Selector="TextBlock">
                            <Setter Property="Margin" Value="0,0,5,0" />
                            <Setter Property="FontSize" Value="15"/>
                        </Style>
                    </StackPanel.Resources>
                    <Image Margin="0,0,10,0" Source="{StaticResource DialogIcon}" />                    
                    <TextBlock Text="{Binding Title}" />                                            
                </StackPanel>
            </Border>
            <Border Grid.Row="1" Background="#333">
                <Grid Margin="20,15,20,15">
                    
                    <Border BorderBrush="#000" BorderThickness="4" CornerRadius="4">
                        <ContentControl  Content="{Binding Activation}" />
                    </Border>
                    
                    <Border Width="180" Height="150" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="0,40,30,0">
                        <Viewbox VerticalAlignment="Center" HorizontalAlignment="Right">
                            <ItemsControl ItemsSource="{Binding Activation.Targets}">

                                <ItemsControl.Resources>
                                    <DataTemplate DataType="{x:Type CrystalArena:Player}">
                                        <Viewbox>
                                            <Border Width="290" >
                                                <Image Source="{Binding AvatarId, Converter={x:Static UserInterface:Converters.AvatarIdToAvatarImage}}"/>
                                            </Border>
                                        </Viewbox>
                                    </DataTemplate>
                                    <DataTemplate DataType="{x:Type CrystalArena:Effect}">
                                        <ContentControl Content="{Binding Source}" />
                                    </DataTemplate>
                                </ItemsControl.Resources>

                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Horizontal" />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>

                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border VerticalAlignment="Center" Margin="10,0,0,0" BorderBrush="#fff" BorderThickness="10" CornerRadius="4" Background="#cc000000">
                                            <ContentControl>
                                                <ContentControl Content="{Binding}">
                                                    <i:Interaction.Triggers>
                                                        
                                                    </i:Interaction.Triggers>
                                                </ContentControl>
                                            </ContentControl>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Viewbox>
                    </Border>
                </Grid>
            </Border>

        </Grid>
    </Border>
</UserControl>
